# GEOスコアリングツール v2.1 テスト用依存パッケージ

# テストフレームワーク
pytest>=8.0.0
pytest-cov>=4.1.0          # カバレッジレポート
pytest-mock>=3.12.0        # モック機能強化
pytest-asyncio>=0.23.0     # 非同期テスト対応（将来版）
pytest-xdist>=3.5.0        # 並列テスト実行

# 既存の依存パッケージ（requirements.txtから）
google-genai>=1.0.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
streamlit>=1.30.0
plotly>=5.18.0
pandas>=2.0.0
pyyaml>=6.0.0
python-dotenv>=1.0.0
apscheduler>=3.10.0

# v2.1 新規追加パッケージ
anthropic>=0.18.0          # Claude API
openai>=1.12.0             # ChatGPT o1 API
serpapi>=0.1.5             # Serp API
httpx>=0.26.0              # HTTP クライアント（MCP用）

# テスト用モック・スタブ
responses>=0.24.0          # HTTPモック
faker>=22.0.0              # テストデータ生成

# コード品質チェック（オプション）
ruff>=0.1.0                # Linter & Formatter
mypy>=1.8.0                # 型チェック

# PostgreSQL対応（本番環境用）
psycopg2-binary>=2.9.9     # PostgreSQLアダプター
